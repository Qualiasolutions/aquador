---
phase: 02-success-pages-email-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/checkout/session-details/route.ts
  - src/app/create-perfume/success/success-content.tsx
  - src/app/checkout/success/page.tsx
autonomous: true

must_haves:
  truths:
    - "Custom perfume success page shows order confirmation after successful payment"
    - "Cart checkout success page displays purchased items, total, and order number"
    - "Custom perfume success page displays perfume composition, size, and order number"
  artifacts:
    - path: "src/app/create-perfume/success/success-content.tsx"
      provides: "Fetches Stripe session via session_id param and displays perfume details"
      min_lines: 100
    - path: "src/app/checkout/success/page.tsx"
      provides: "Fetches Stripe session via session_id param and displays cart items"
      min_lines: 150
  key_links:
    - from: "src/app/create-perfume/success/success-content.tsx"
      to: "/api/checkout/session-details"
      via: "fetch with session_id"
      pattern: "fetch.*session_id"
    - from: "src/app/checkout/success/page.tsx"
      to: "/api/checkout/session-details"
      via: "fetch with session_id"
      pattern: "fetch.*session_id"
---

<objective>
Fix both success pages to correctly detect successful payment and display order details from Stripe session.

Purpose: Close PAY-01 (custom perfume success page detection), PAY-02 (order details display). Customers currently see error state or generic messages instead of their order confirmation. Success pages are the only proof customers have that their payment worked.

Output: Working success pages that fetch session data and show order confirmation with items, total, and order number.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior work context
@.planning/phases/01-checkout-security-validation/01-02-SUMMARY.md

# Source files to modify
@src/app/create-perfume/success/success-content.tsx
@src/app/checkout/success/page.tsx
@src/app/api/checkout/route.ts
@src/app/api/create-perfume/payment/route.ts
@src/app/api/webhooks/stripe/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create session details API route</name>
  <files>src/app/api/checkout/session-details/route.ts</files>
  <action>
Create GET endpoint that retrieves Stripe Checkout Session details and returns order information.

Implementation:
- Accept `session_id` query parameter
- Fetch session via `stripe.checkout.sessions.retrieve(sessionId, { expand: ['line_items'] })`
- Parse session data into structured response:
  - For cart checkout (metadata.items exists): Parse shortened metadata (pid, vid, qty), reconstruct full item details from product catalog using getProductById
  - For custom perfume (metadata.productType === 'custom-perfume'): Build item from metadata fields (perfumeName, topNote, heartNote, baseNote, volume)
- Return: `{ sessionId, orderNumber, items: Array<{name, quantity, price, size?, composition?}>, total, currency, shippingAddress?, createdAt }`
- Order number: Last 8 chars of session.id uppercase (e.g., #4WC00000)
- Handle errors: 400 if no session_id, 404 if session not found, 500 on Stripe errors
- Rate limit: Use checkRateLimit(request, 'session-details')
- Security: Only return data for completed sessions (session.payment_status === 'paid')

Import pattern from webhook handler for metadata parsing (Phase 1 established shortened key pattern).
  </action>
  <verify>
TypeScript compilation passes:
```bash
npx tsc --noEmit
```

Test endpoint with valid session_id:
```bash
# Get a real session_id from Stripe test mode or recent webhook logs
curl -s "http://localhost:3000/api/checkout/session-details?session_id=cs_test_..." | jq
```

Expected response structure exists (sessionId, orderNumber, items array, total).
  </verify>
  <done>
- API route exists at src/app/api/checkout/session-details/route.ts
- Returns 400 for missing session_id
- Returns 404 for invalid session_id
- Returns 200 with order data for valid paid sessions
- Correctly parses both cart and custom perfume metadata formats
- Order number matches format #XXXXXXXX (8 uppercase chars)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix custom perfume success page to use session_id parameter</name>
  <files>src/app/create-perfume/success/success-content.tsx</files>
  <action>
Replace broken redirect_status detection with session-based data fetching.

Changes:
1. Update useEffect to:
   - Get `session_id` from searchParams (not redirect_status)
   - If no session_id: setStatus('error')
   - If session_id exists: fetch `/api/checkout/session-details?session_id=${sessionId}`
   - On success: setStatus('success') + store order data in state
   - On error: setStatus('error')

2. Add state for order data:
   ```typescript
   const [orderData, setOrderData] = useState<{
     orderNumber: string;
     perfumeName: string;
     composition: { top: string; heart: string; base: string };
     volume: string;
     total: number;
   } | null>(null);
   ```

3. Update success content to display:
   - Order number (e.g., "Order #4WC00000")
   - Perfume name
   - Composition: "Top: {top} / Heart: {heart} / Base: {base}"
   - Volume (50ml or 100ml)
   - Total price (formatted with formatPrice from @/lib/utils)
   - Keep existing "You will receive an email confirmation" message

4. Keep error and loading states as-is (they work correctly)

Match the luxury design aesthetic (amber/gold accent colors, existing card styling).
  </action>
  <verify>
TypeScript compilation passes:
```bash
npx tsc --noEmit
```

Manual test (requires test mode Stripe session):
1. Start dev server: `npm run dev`
2. Create a custom perfume and complete test checkout
3. After redirect to success page, verify:
   - Success state shows (not error)
   - Order number displays
   - Perfume details (name, composition, volume) are visible
   - Price displays correctly
   - No console errors
  </verify>
  <done>
- Component fetches session data using session_id parameter
- Success state displays order number, perfume composition, volume, and total
- Error state still shows when session_id is missing or invalid
- No TypeScript errors
- Follows existing luxury design patterns (amber/gold colors, card styling)
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix cart checkout success page to display order details</name>
  <files>src/app/checkout/success/page.tsx</files>
  <action>
Replace generic "Thank You" page with order details display.

Changes:
1. Add useSearchParams hook: `const searchParams = useSearchParams()`

2. Add state:
   ```typescript
   const [loading, setLoading] = useState(true);
   const [orderData, setOrderData] = useState<{
     orderNumber: string;
     items: Array<{ name: string; quantity: number; price: number; size: string }>;
     total: number;
     currency: string;
   } | null>(null);
   const [error, setError] = useState(false);
   ```

3. Replace clearCart useEffect with data-fetching logic:
   - Get session_id from searchParams
   - If no session_id: setError(true) + setLoading(false)
   - If session_id: fetch `/api/checkout/session-details?session_id=${sessionId}`
   - On success: setOrderData(response) + clearCart() + setLoading(false)
   - On error: setError(true) + setLoading(false)

4. Update JSX to show three states:
   - Loading: Existing CheckCircle animation + "Processing..." text
   - Error: "Unable to load order details" + "Contact us" button
   - Success: Display:
     - Order number at top (large, prominent)
     - Items list in table format: Product name | Size | Qty | Price (per item)
     - Subtotal, Shipping (FREE), Total
     - Shipping timeline: "3-7 business days"
     - Keep existing "What happens next?" section

5. Preserve existing animations (motion from framer-motion)

Match existing dark theme design (bg-gold-ambient, border-gold/20, etc.).
  </action>
  <verify>
TypeScript compilation passes:
```bash
npx tsc --noEmit
```

Manual test (requires test mode Stripe session):
1. Add items to cart in dev mode
2. Complete test checkout
3. Verify success page shows:
   - Order number
   - All cart items with correct names, sizes, quantities, prices
   - Correct total amount
   - Free shipping messaging
   - No console errors
4. Test error case: Visit /checkout/success without session_id param → should show error state
  </verify>
  <done>
- Page fetches and displays order details from session_id parameter
- Shows order number, items table, and total
- Error state handles missing/invalid session_id
- Cart clears only after successful data fetch
- Follows existing design system (dark theme, gold accents, motion animations)
- No TypeScript errors
  </done>
</task>

</tasks>

<verification>
Run TypeScript compiler after all changes:
```bash
npx tsc --noEmit
```

Test both success flows end-to-end in Stripe test mode:
1. Custom perfume flow: Create → Pay → Success page shows perfume details
2. Cart checkout flow: Add items → Checkout → Success page shows item list

Both pages should:
- Display order confirmation (not error state)
- Show order number in format #XXXXXXXX
- Display what customer ordered (perfume composition OR cart items)
- Show total amount paid
- Have no console errors
</verification>

<success_criteria>
- [x] Custom perfume success page correctly detects successful payment via session_id parameter (not redirect_status)
- [x] Custom perfume success page displays: order number, perfume name, composition, volume, total
- [x] Cart checkout success page displays: order number, item list with sizes/quantities, total
- [x] Both pages handle error cases (missing session_id) gracefully
- [x] Session details API route exists and returns structured order data
- [x] TypeScript compilation passes with no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-success-pages-email-reliability/02-01-SUMMARY.md`
</output>
