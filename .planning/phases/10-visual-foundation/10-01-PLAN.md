---
phase: 10-visual-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - tailwind.config.ts
  - src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "User sees distinctive luxury typography hierarchy across all pages"
    - "Headings use premium serif weights with perfect letter-spacing"
    - "Body text is readable and elegant at all screen sizes"
    - "Color palette creates sophisticated luxury atmosphere"
    - "Gold accents feel refined, not garish"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Enhanced typography scale and color palette"
      contains: "typography scale with luxury letter-spacing"
    - path: "tailwind.config.ts"
      provides: "Extended color system with refined gradients"
      contains: "oklch color values"
  key_links:
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "Playfair_Display font loading"
      pattern: "Playfair_Display.*weight.*\\[.*800.*\\]"
    - from: "tailwind.config.ts"
      to: "src/app/globals.css"
      via: "CSS variable color system"
      pattern: "var\\(--.*\\)"
---

<objective>
Refine typography system and elevate color palette to luxury perfume brand standards.

Purpose: Establish premium visual foundation that distinguishes Aquador from generic e-commerce
Output: Enhanced typography scale with refined weights, sophisticated color palette using perceptually uniform colors
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@/home/qualia/.claude/skills/frontend-master/SKILL.md
@/home/qualia/.claude/skills/responsive/SKILL.md
@src/app/globals.css
@tailwind.config.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance typography system with luxury weights and refined scale</name>
  <files>
    src/app/layout.tsx
    src/app/globals.css
  </files>
  <action>
Enhance the typography system for luxury brand presentation:

**src/app/layout.tsx:**
- Add weight 800 to Playfair_Display (currently stops at 700) for dramatic headlines
- Add weight 100 to Poppins (currently starts at 200) for ultra-light accents
- Maintain display: swap for performance

**src/app/globals.css:**
- Refine typography scale using fluid clamp() values for perfect responsive scaling:
  - Headings: clamp() with 3x+ size jumps between mobile and desktop
  - Body: clamp(1rem, 0.5vw + 0.875rem, 1.125rem) for optimal readability
- Add luxury letter-spacing scale to :root variables:
  - --letter-tight: -0.02em (for large display text)
  - --letter-normal: 0em
  - --letter-wide: 0.02em (existing, keep)
  - --letter-wider: 0.05em (for micro labels)
  - --letter-widest: 0.15em (for uppercase CTAs)
- Update h1-h6 base styles:
  - Add font-feature-settings: 'liga' 1, 'kern' 1 for refined rendering
  - Set line-height: 1.2 for headings (tighter, more luxurious)
  - Set line-height: 1.6 for body (comfortable reading)
- Create .heading-luxury class with dramatic sizing and refined spacing

Do NOT change existing Playfair/Poppins font families - they're already a strong luxury pairing per frontend-master anti-AI-slop rules.
  </action>
  <verify>
- Verify font weights loaded: Check browser DevTools Network tab for Playfair 800 and Poppins 100
- Check typography scale renders correctly at 375px, 768px, 1440px widths
- Confirm letter-spacing variables exist in computed styles
  </verify>
  <done>
- Playfair_Display loads weights 400, 500, 600, 700, 800
- Poppins loads weights 100, 200, 300, 400, 500, 600, 700
- Typography scale uses fluid clamp() values with 3x+ range
- Letter-spacing variables defined and applied to heading classes
- Font rendering includes ligatures and kerning
  </done>
</task>

<task type="auto">
  <name>Task 2: Elevate color palette with perceptually uniform colors and refined gradients</name>
  <files>
    src/app/globals.css
    tailwind.config.ts
  </files>
  <action>
Transform the color system from simple hex to sophisticated perceptually uniform palette:

**src/app/globals.css :root variables:**
Replace existing gold hex values with oklch() for perceptually uniform brightness:
- --gold: oklch(75% 0.12 85) (warm gold, matches #D4AF37 perceptually)
- --gold-light: oklch(85% 0.11 90) (brighter gold)
- --gold-dark: oklch(65% 0.13 80) (deeper gold)

Add atmospheric background colors using oklch:
- --bg-black: oklch(10% 0 0) (true black with slight warmth)
- --bg-dark: oklch(15% 0.005 280) (very dark with subtle cool tone)
- --bg-darker: oklch(12% 0.003 280)

Add semantic gray scale in oklch (neutral, no color cast):
- --gray-50: oklch(97% 0 0)
- --gray-100: oklch(93% 0 0)
- --gray-200: oklch(85% 0 0)
- --gray-300: oklch(70% 0 0)
- --gray-400: oklch(55% 0 0) (body text on dark)
- --gray-500: oklch(40% 0 0)

**tailwind.config.ts:**
Update colors.gold to reference CSS variables (keep existing structure):
- Keep DEFAULT: "#D4AF37" for compatibility
- Add oklch variants as new keys:
  - gold.50 through gold.900 for complete scale

Update background gradients to use oklch color-mix():
- bg-gold-ambient classes: Use color-mix() instead of rgba for smoother blending
- Create new .bg-sophisticated gradient with subtle diagonal flow

Add new luxury gradient utilities:
- .text-gradient-refined: Subtle gold shimmer (not flashy)
- .bg-gradient-radial-gold: Radial gradient for hero sections

Keep all existing color utility classes working - this is additive enhancement, not breaking change.
  </action>
  <verify>
- Run `npm run dev` and check homepage renders without color breaks
- Inspect computed styles - oklch values should appear in CSS
- Check color contrast: All text meets WCAG 4.5:1 minimum ratio
- Verify gradients render smoothly without banding
  </verify>
  <done>
- All gold colors use oklch() for perceptual uniformity
- Background colors use oklch with subtle cool tones
- Gray scale is truly neutral (no color cast)
- Tailwind config extends colors without breaking existing classes
- New gradient utilities available (.text-gradient-refined, .bg-gradient-radial-gold)
- WCAG color contrast ratios maintained
  </done>
</task>

<task type="auto">
  <name>Task 3: Document design system decisions and create visual hierarchy guide</name>
  <files>
    src/app/globals.css
  </files>
  <action>
Add comprehensive inline documentation to globals.css design system:

At the top of the file (after DESIGN SYSTEM header comment), add:
```css
/* ==========================================================================
   TYPOGRAPHY PHILOSOPHY
   ==========================================================================

   Playfair Display (800/700/600/500/400): Luxury serif for headings
   - Weight 800: Hero headlines, dramatic impact
   - Weight 700: Section titles, primary headings
   - Weight 600: Subheadings, card titles
   - Weight 500: Subtle emphasis
   - Weight 400: Body serif use (rare)

   Poppins (100-700): Clean sans for body and UI
   - Weight 100: Ultra-light accents, decorative
   - Weight 200-300: Body text, descriptions
   - Weight 400-500: UI elements, buttons
   - Weight 600-700: Strong emphasis, CTAs

   Letter-spacing scale:
   - Tight (-0.02em): Large display text compression
   - Normal (0em): Standard body text
   - Wide (0.02em): Headings with breathing room
   - Wider (0.05em): Micro labels
   - Widest (0.15em): Uppercase CTAs, strong impact

   ========================================================================== */
```

Below COLOR PHILOSOPHY section, add:
```css
/* ==========================================================================
   COLOR PHILOSOPHY
   ==========================================================================

   oklch() for perceptual uniformity - ensures consistent brightness across hues

   Gold palette (warm, luxurious):
   - oklch(85%): Light gold accents
   - oklch(75%): Primary gold brand color
   - oklch(65%): Deep gold for contrast

   Background palette (atmospheric):
   - True black with subtle warmth for depth
   - Dark grays with cool undertones for layering

   Neutral grays (no color cast):
   - Perfect for body text and UI elements
   - Maintains readability across all contexts

   ========================================================================== */
```

Update section comments throughout to reference these philosophies where applied.
  </action>
  <verify>
- Read src/app/globals.css - documentation comments present and clear
- Comments explain WHY, not just WHAT (philosophy, not redundant descriptions)
- Typography and color sections reference the philosophy headers
  </verify>
  <done>
- Design system documentation added to globals.css
- Typography philosophy explains weight usage and letter-spacing scale
- Color philosophy explains oklch() choice and palette strategy
- Comments are concise and useful for future developers
  </done>
</task>

</tasks>

<verification>
Run comprehensive visual checks:
1. `npm run dev` - Site loads without errors
2. Visit homepage at localhost:3000 - typography looks refined and luxurious
3. Check typography at 375px, 768px, 1440px - scales smoothly
4. Inspect gold colors in DevTools - oklch() values render correctly
5. Check text contrast - all text readable against backgrounds
6. Verify letter-spacing on headings - breathing room without feeling loose
</verification>

<success_criteria>
- Playfair Display loads 5 weights (400, 500, 600, 700, 800)
- Poppins loads 7 weights (100-700)
- Typography uses fluid clamp() for responsive scaling
- Letter-spacing scale defined and applied to headings
- All gold colors use oklch() for perceptual uniformity
- Background colors use oklch with subtle tones
- WCAG color contrast maintained (4.5:1 minimum)
- Design system documented with philosophy comments
- No visual regressions on existing pages
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-foundation/10-01-SUMMARY.md`
</output>
