---
phase: 14-3d-product-showcase
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/three/config.ts
  - src/components/3d/ProductViewer.tsx
  - src/app/create-perfume/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can click front/side/back angle buttons in the 3D viewer and the camera animates smoothly to that angle"
    - "Mobile users in the custom perfume builder get simplified lighting (no AccumulativeShadows)"
  artifacts:
    - path: "src/lib/three/config.ts"
      provides: "ANGLE_PRESETS constant with front/side/back camera targets"
      contains: "ANGLE_PRESETS"
    - path: "src/components/3d/ProductViewer.tsx"
      provides: "Angle preset buttons overlaid on the 3D viewer with lerp animation"
      exports: ["ProductViewer"]
    - path: "src/app/create-perfume/page.tsx"
      provides: "simplified={isMobile} wired to Lighting in builder"
      contains: "simplified={isMobile}"
  key_links:
    - from: "ProductViewer.tsx angle buttons"
      to: "Scene.tsx controlsRef"
      via: "onAngleSelect callback prop → setAzimuthalAngle/setPolarAngle/update"
      pattern: "setAzimuthalAngle|setPolarAngle"
    - from: "src/app/create-perfume/page.tsx"
      to: "useDeviceCapabilities"
      via: "isMobile destructure → simplified={isMobile}"
      pattern: "simplified=\\{isMobile\\}"
---

<objective>
Close two gaps from the Phase 14 verification: implement angle preset buttons with camera interpolation (3D-05), and fix the builder's hardcoded simplified={false} for mobile (3D-06/PERF-04).

Purpose: The phase goal requires "smooth interpolation between views" — this is the only unimplemented feature. The mobile lighting fix is a one-liner wire-up omission.
Output: Angle preset UI wired to OrbitControls camera animation; builder passes simplified={isMobile} to Lighting.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-3d-product-showcase/14-VERIFICATION.md
@src/lib/three/config.ts
@src/components/3d/Scene.tsx
@src/components/3d/ProductViewer.tsx
@src/app/create-perfume/page.tsx
@~/.claude/skills/frontend-master/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ANGLE_PRESETS to config and wire camera interpolation into ProductViewer</name>
  <files>
    src/lib/three/config.ts
    src/components/3d/ProductViewer.tsx
  </files>
  <action>
**config.ts** — append ANGLE_PRESETS constant after KEYBOARD_CONFIG:

```ts
/**
 * Preset camera angles for the angle-selector UI.
 * azimuth: spherical.theta (radians, 0 = front)
 * polar: spherical.phi (radians, clamped by ORBIT_CONFIG.minPolarAngle/maxPolarAngle)
 */
export const ANGLE_PRESETS = [
  { id: 'front', label: 'Front', azimuth: 0,              polar: Math.PI / 3 },
  { id: 'side',  label: 'Side',  azimuth: Math.PI / 2,    polar: Math.PI / 3 },
  { id: 'back',  label: 'Back',  azimuth: Math.PI,         polar: Math.PI / 3 },
] as const;

export type AnglePresetId = typeof ANGLE_PRESETS[number]['id'];
```

**ProductViewer.tsx** — thread an `onAngleSelect` callback into Scene and render the preset buttons as an absolute overlay:

1. Import `ANGLE_PRESETS`, `AnglePresetId` from `@/lib/three/config`.
2. Import `useState`, `useCallback`, `useRef` from react.
3. Import `track3DInteraction` from `@/lib/analytics/engagement-tracker`.
4. Add `activeAngle` state (`AnglePresetId | null`, initial `null`).
5. Add `sceneControlsRef` typed as `React.MutableRefObject<any>` to hold the forwarded OrbitControls ref from Scene. Because Scene owns `controlsRef` internally, expose a forwarding mechanism: pass an `onControlsReady` callback prop to Scene that receives the OrbitControls instance. Scene already passes `controlsRef` to `<OrbitControls ref={controlsRef}>`  — add an optional `onControlsReady?: (ctrl: any) => void` prop to Scene and call it inside a `useEffect` after `controlsRef.current` becomes available.
6. In `ProductViewer3DContent`, receive the `onAngleSelect` prop. On button click:
   - Get the OrbitControls instance from the forwarded ref.
   - Call `ctrl.setAzimuthalAngle(preset.azimuth)` and `ctrl.setPolarAngle(preset.polar)` — these are the same internal methods already used by Scene's keyboard handler (`handleKeyRotate`).
   - Call `ctrl.update()` to apply immediately. OrbitControls `enableDamping` (dampingFactor 0.05) provides the smooth lerp automatically on the next render frames — no manual lerp loop needed.
   - Track: `track3DInteraction('angle_preset', { productName, angle: preset.id })`.
   - Set `activeAngle` to `preset.id`.
7. Render preset buttons as an absolutely-positioned row at the bottom of the viewer div (inside the wrapper div, below the Scene but within the `relative` container). Use gold color palette and Poppins font matching the design system. Style: small pill buttons, gold border, transparent background, gold text. Active state: gold background, dark text. Hover: gold/20 background.

Button layout (Tailwind):
```
absolute bottom-3 left-1/2 -translate-x-1/2 flex gap-2 z-10
```

Each button:
```
px-3 py-1 text-xs font-medium font-poppins rounded-full border transition-colors duration-200
border-gold/50 text-gold/80 hover:bg-gold/20
active: bg-gold text-dark-900 border-gold
```

Do NOT add animations beyond the CSS transition — keep it minimal. Do NOT rewrite the Scene component beyond adding the optional `onControlsReady` prop.

**Scene.tsx change** (minimal):
- Add `onControlsReady?: (ctrl: any) => void` to `SceneProps`.
- Add a `useEffect` that fires once `controlsRef.current` is set: `useEffect(() => { if (controlsRef.current && onControlsReady) onControlsReady(controlsRef.current); }, [onControlsReady])`. Place it after the existing `useEffect` at line 41.
- Pass `onControlsReady` through to the inner `<Scene>` call in `ProductViewer3DContent`.
  </action>
  <verify>
    1. `npx tsc --noEmit` passes with no new errors.
    2. Grep confirms `ANGLE_PRESETS` exists in `src/lib/three/config.ts`.
    3. Grep confirms `onControlsReady` appears in `src/components/3d/Scene.tsx`.
    4. Grep confirms `track3DInteraction.*angle_preset` appears in `src/components/3d/ProductViewer.tsx`.
    5. Grep confirms `setAzimuthalAngle` appears in `src/components/3d/ProductViewer.tsx`.
  </verify>
  <done>
    Three pill buttons (Front / Side / Back) are rendered over the 3D viewer. Clicking one calls setAzimuthalAngle/setPolarAngle on the live OrbitControls instance and OrbitControls damping smoothly transitions the camera to the target angle. Active button shows gold background. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix builder mobile lighting — pass simplified={isMobile} in create-perfume/page.tsx</name>
  <files>src/app/create-perfume/page.tsx</files>
  <action>
Line 321 of `src/app/create-perfume/page.tsx` reads:

```tsx
<Lighting simplified={false} />
```

The page already imports nothing from `useDeviceCapabilities`. Find the component that renders the `<Scene>` block (it is an inline render inside the page component's JSX). Determine if the page component is already a client component (it has `'use client'` — confirm with a read). If so:

1. Add import: `import { useDeviceCapabilities } from '@/hooks/useDeviceCapabilities';`
2. Destructure inside the component body: `const { isMobile } = useDeviceCapabilities();`
3. Change line 321 to: `<Lighting simplified={isMobile} />`

If the Scene block is rendered inside a Server Component section, extract it into a small `'use client'` sub-component that wraps the Scene and uses the hook — but only if necessary. Read the top of the file first to confirm the existing directive.

Do not change anything else in the file.
  </action>
  <verify>
    1. `npx tsc --noEmit` passes.
    2. `grep -n "simplified" src/app/create-perfume/page.tsx` shows `simplified={isMobile}` at the relevant line (not `simplified={false}`).
    3. `grep -n "useDeviceCapabilities" src/app/create-perfume/page.tsx` confirms the import is present.
  </verify>
  <done>
    Mobile users in the custom perfume builder receive `simplified={true}` (no AccumulativeShadows), reducing GPU load. Desktop users receive `simplified={false}` unchanged. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
After both tasks:

```bash
npx tsc --noEmit
npm run lint
```

Both must pass with no new errors or warnings.

Spot checks:
- `grep "ANGLE_PRESETS" src/lib/three/config.ts` — present
- `grep "simplified={isMobile}" src/app/create-perfume/page.tsx` — present
- `grep "setAzimuthalAngle" src/components/3d/ProductViewer.tsx` — present
</verification>

<success_criteria>
1. Three angle preset buttons (Front/Side/Back) render as a gold pill-button row overlaid at the bottom of the ProductViewer 3D canvas.
2. Clicking a preset button smoothly animates the camera to that angle via OrbitControls damping.
3. Active preset button shows gold background / dark text.
4. `create-perfume/page.tsx` line ~321 reads `simplified={isMobile}` (not `simplified={false}`).
5. `npx tsc --noEmit` and `npm run lint` both pass.
</success_criteria>

<output>
After completion, create `.planning/phases/14-3d-product-showcase/14-05-SUMMARY.md` documenting:
- What was built (angle presets, mobile lighting fix)
- Files modified
- Key decisions (used OrbitControls damping for interpolation rather than manual lerp loop)
- TypeScript and lint results
</output>
