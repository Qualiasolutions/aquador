---
phase: 15-immersive-navigation-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shop/AnimatedFilterBar.tsx
  - src/components/shop/CategoryTransition.tsx
  - src/lib/animations/filter-transitions.ts
  - src/app/shop/ShopContent.tsx
  - src/app/shop/[category]/CategoryContent.tsx
  - src/app/shop/[category]/page.tsx
autonomous: true

must_haves:
  truths:
    - "User sees smooth animated transitions when changing category or type filters"
    - "User experiences layout animation when product grid updates after filtering"
    - "User sees seamless visual continuity when navigating between category pages"
    - "User on mobile experiences parallax-safe filter animations"
  artifacts:
    - path: "src/components/shop/AnimatedFilterBar.tsx"
      provides: "Animated filter pill bar with active state transitions"
      exports: ["AnimatedFilterBar", "AnimatedFilterBarProps"]
      min_lines: 80
    - path: "src/components/shop/CategoryTransition.tsx"
      provides: "Shared layout animation wrapper for category page transitions"
      exports: ["CategoryTransition"]
      min_lines: 40
    - path: "src/lib/animations/filter-transitions.ts"
      provides: "Filter animation variants and grid layout transition config"
      exports: ["filterVariants", "gridLayoutTransition", "FILTER_TIMING"]
  key_links:
    - from: "src/components/shop/AnimatedFilterBar.tsx"
      to: "src/lib/animations/filter-transitions.ts"
      via: "animation variant consumption"
      pattern: "filterVariants"
    - from: "src/app/shop/ShopContent.tsx"
      to: "src/components/shop/AnimatedFilterBar.tsx"
      via: "component integration"
      pattern: "AnimatedFilterBar"
    - from: "src/app/shop/[category]/CategoryContent.tsx"
      to: "src/components/shop/CategoryTransition.tsx"
      via: "page transition wrapper"
      pattern: "CategoryTransition"
---

<objective>
Build animated product filtering and seamless category transitions for the shop experience.

Purpose: Transform static filter clicks and page navigations into fluid, cinematic interactions that feel luxurious. Product grid items animate in/out when filters change. Category pages transition with visual continuity.

Output: AnimatedFilterBar component, CategoryTransition wrapper, filter animation library, integrated into ShopContent and CategoryContent pages.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@~/.claude/skills/frontend-master/SKILL.md
@~/.claude/skills/responsive/SKILL.md
@src/app/shop/ShopContent.tsx
@src/app/shop/[category]/CategoryContent.tsx
@src/app/shop/[category]/page.tsx
@src/components/ui/ProductCard.tsx
@src/lib/animations/scroll-animations.ts
@src/lib/categories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create filter animation library and AnimatedFilterBar component</name>
  <files>
src/lib/animations/filter-transitions.ts
src/components/shop/AnimatedFilterBar.tsx
  </files>
  <action>
Create `src/lib/animations/filter-transitions.ts` with filter-specific animation config:
- Export `FILTER_TIMING` object: `{ pill: 0.25, grid: 0.35, stagger: 0.03 }` — fast, responsive feel
- Export `filterVariants` for AnimatePresence on filter pills:
  - `inactive`: scale(1), no shadow, border gold-500/20
  - `active`: scale(1), gold background, shadow-lg shadow-gold-500/20
  - Transition: spring stiffness 400, damping 25 (snappy, luxury feel)
- Export `gridLayoutTransition` for Framer Motion `layout` prop on product grid items:
  - Type spring, stiffness 300, damping 30 — smooth grid reflow without bounce
- Export `gridItemVariants` for staggered product card entry:
  - `hidden`: opacity 0, y 15, scale 0.97
  - `visible`: opacity 1, y 0, scale 1
  - Transition: duration 0.3, ease [0.22, 1, 0.36, 1] (matching Phase 12 EXPO_EASE)
- Export `gridExitVariants`: opacity 0, scale 0.95, transition duration 0.15 (fast exit)

Create `src/components/shop/AnimatedFilterBar.tsx`:
- Props: `filters: Array<{ id: string; label: string }>`, `activeFilter: string | null`, `onFilterChange: (id: string | null) => void`, `className?: string`
- Renders horizontal scrollable pill bar (overflow-x-auto, hide scrollbar with `scrollbar-none`)
- Each pill is a `motion.button` with `layoutId` for shared layout animation of active indicator
- Active pill gets gold-500 background with `layoutId="activeFilter"` animated underline/bg
- Use Framer Motion `AnimatePresence` for pill state transitions
- Include "All" pill as first item (active when activeFilter is null)
- Mobile: full-width horizontal scroll with snap-x snap-mandatory for touch scrolling
- Desktop: centered flex-wrap layout
- Min-height 44px on all pills (WCAG touch target)
- Typography: text-[10.5px] uppercase tracking-[0.15em] (matching Navbar style)
- Separate variant for "type" sub-filters (smaller, text-only: text-[10px])
- Export `AnimatedTypeFilter` sub-component for product type filtering
  </action>
  <verify>
Run `npx tsc --noEmit` to verify TypeScript compilation.
Check exports:
```bash
grep -E "export" src/lib/animations/filter-transitions.ts
grep -E "export" src/components/shop/AnimatedFilterBar.tsx
```
  </verify>
  <done>
- filter-transitions.ts exports FILTER_TIMING, filterVariants, gridLayoutTransition, gridItemVariants, gridExitVariants
- AnimatedFilterBar.tsx exports component with proper TypeScript interfaces
- Pills have shared layout animation for active state
- TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate animated filters into ShopContent and add category transitions</name>
  <files>
src/components/shop/CategoryTransition.tsx
src/app/shop/ShopContent.tsx
src/app/shop/[category]/CategoryContent.tsx
src/app/shop/[category]/page.tsx
  </files>
  <action>
Create `src/components/shop/CategoryTransition.tsx`:
- Wrapper component using Framer Motion `AnimatePresence` mode="wait"
- Accepts `categorySlug: string` as key for transition detection
- Entry animation: opacity 0 -> 1, y 20 -> 0, duration 0.4
- Exit animation: opacity 1 -> 0, y -10, duration 0.2 (fast exit)
- Easing: [0.22, 1, 0.36, 1] (EXPO_EASE from Phase 12)
- Wraps children in `motion.div` with key={categorySlug}

Update `src/app/shop/ShopContent.tsx`:
- Replace existing plain filter buttons with `AnimatedFilterBar` for category filters
- Replace existing type filter buttons with `AnimatedTypeFilter`
- Wrap product grid with `AnimatePresence` mode="popLayout" to animate items in/out on filter change
- Add `layout` prop to each product card wrapper `motion.div` using `gridLayoutTransition`
- Use `gridItemVariants` for staggered entry (variants prop on motion.div)
- Wrap grid in `motion.div` with `key={filterKey}` where filterKey derives from selectedCategory+selectedType+searchQuery
- Keep all existing search, filter logic, and ProductCard usage — only replace JSX structure for filters and grid animation
- Remove inline Tailwind transition classes on filter buttons (now handled by Framer Motion)

Update `src/app/shop/[category]/CategoryContent.tsx`:
- Wrap entire content in `CategoryTransition` with `categorySlug={category.slug}`
- Add `layout` prop to product grid items for smooth reflow during search filtering
- Use `gridItemVariants` for staggered product entry on page load
- Keep existing search bar, product grid, and empty state logic intact

Update `src/app/shop/[category]/page.tsx`:
- Read current structure first
- Ensure CategoryContent receives category slug for transition key
- No major changes expected — just verify props flow correctly
  </action>
  <verify>
Run dev server:
```bash
npm run dev
```
1. Visit http://localhost:3000/shop — click category filters, verify smooth gold pill transitions
2. Click type filters — verify product grid animates (items fade/slide in)
3. Navigate between /shop/women and /shop/men — verify page content transitions smoothly
4. Test on mobile viewport (375px) — verify filter pills scroll horizontally with snap
5. Run `npx tsc --noEmit` — no TypeScript errors
6. Run `npm run lint` — no lint errors
  </verify>
  <done>
- ShopContent uses AnimatedFilterBar with animated active states
- Product grid has layout animations and staggered entry on filter changes
- CategoryContent has CategoryTransition wrapper
- Category pages transition smoothly between each other
- Mobile filter bar scrolls horizontally with touch snap
- All existing functionality preserved (search, filtering, ProductCard display)
- TypeScript and lint pass
  </done>
</task>

</tasks>

<verification>
Manual verification:
1. Visit /shop — click "Women" filter — products animate with staggered fade-in, grid reflows smoothly
2. Click "Men" filter — previous products exit quickly, new products enter with stagger
3. Click "All" — smooth transition back to full grid
4. Click type filters (Perfume, Essence Oil) — products filter with animated layout
5. Navigate to /shop/women — page content transitions in with fade+slide
6. Navigate to /shop/men — previous content exits, new content enters (visual continuity)
7. Mobile viewport — filter pills are horizontally scrollable with snap
8. Verify 60fps performance during filter transitions (Chrome DevTools Performance tab)
9. Reduced motion — verify animations are simplified (instant transitions)

Automated:
```bash
npx tsc --noEmit
npm run lint
npm test
```
</verification>

<success_criteria>
Phase 15 Plan 01 complete when:
- [x] AnimatedFilterBar renders pills with shared layout animation for active state
- [x] Product grid items animate in/out when filters change (stagger + layout)
- [x] Category page transitions have visual continuity (fade+slide)
- [x] Filter pills are horizontally scrollable on mobile with snap
- [x] All filter pills meet 44px minimum touch target
- [x] Animations match luxury timing (spring, EXPO_EASE)
- [x] Existing search and filter logic preserved
- [x] TypeScript compiles, lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-immersive-navigation-discovery/15-01-SUMMARY.md`
</output>
