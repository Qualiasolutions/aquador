---
phase: 15-immersive-navigation-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ProductCard.tsx
  - src/components/shop/ProductQuickView.tsx
  - src/components/shop/DiscoveryGrid.tsx
  - src/lib/animations/discovery-animations.ts
autonomous: true

must_haves:
  truths:
    - "User encounters contextual hover states that reveal additional product information naturally"
    - "User discovers products through immersive browsing patterns with progressive disclosure"
    - "User sees product details progressively revealed on hover without leaving the grid"
    - "User on mobile sees progressive disclosure through tap interaction instead of hover"
  artifacts:
    - path: "src/components/shop/ProductQuickView.tsx"
      provides: "Hover-activated quick view overlay revealing product details"
      exports: ["ProductQuickView"]
      min_lines: 60
    - path: "src/components/shop/DiscoveryGrid.tsx"
      provides: "Enhanced product grid with progressive disclosure and immersive browsing"
      exports: ["DiscoveryGrid", "DiscoveryGridProps"]
      min_lines: 80
    - path: "src/lib/animations/discovery-animations.ts"
      provides: "Animation variants for hover reveals, progressive disclosure"
      exports: ["hoverRevealVariants", "progressiveDisclosureVariants", "DISCOVERY_TIMING"]
  key_links:
    - from: "src/components/shop/ProductQuickView.tsx"
      to: "src/lib/animations/discovery-animations.ts"
      via: "animation variant consumption"
      pattern: "hoverRevealVariants"
    - from: "src/components/shop/DiscoveryGrid.tsx"
      to: "src/components/shop/ProductQuickView.tsx"
      via: "quick view integration on product cards"
      pattern: "ProductQuickView"
    - from: "src/components/ui/ProductCard.tsx"
      to: "src/components/shop/ProductQuickView.tsx"
      via: "hover state integration"
      pattern: "ProductQuickView"
---

<objective>
Create immersive product discovery with contextual hover states and progressive disclosure that reveal product information naturally as users browse.

Purpose: Transform the flat product grid into an engaging discovery experience where hovering reveals additional context (notes, quick-add, description preview) without navigating away. Mobile uses tap-to-reveal pattern instead.

Output: ProductQuickView overlay component, DiscoveryGrid with progressive disclosure, enhanced ProductCard hover states, discovery animation library.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@~/.claude/skills/frontend-master/SKILL.md
@~/.claude/skills/responsive/SKILL.md
@src/components/ui/ProductCard.tsx
@src/app/shop/ShopContent.tsx
@src/lib/supabase/types.ts
@src/lib/categories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create discovery animation library and ProductQuickView</name>
  <files>
src/lib/animations/discovery-animations.ts
src/components/shop/ProductQuickView.tsx
  </files>
  <action>
Create `src/lib/animations/discovery-animations.ts`:
- Export `DISCOVERY_TIMING`: `{ hoverDelay: 0.15, revealDuration: 0.35, exitDuration: 0.2 }`
- Export `hoverRevealVariants`:
  - `hidden`: opacity 0, y 8, filter 'blur(4px)'
  - `visible`: opacity 1, y 0, filter 'blur(0px)', transition duration 0.35 ease [0.22, 1, 0.36, 1]
  - `exit`: opacity 0, y -4, transition duration 0.15
- Export `progressiveDisclosureVariants`:
  - `collapsed`: height 0, opacity 0
  - `expanded`: height 'auto', opacity 1, transition duration 0.4 ease [0.22, 1, 0.36, 1]
- Export `imageZoomVariants`:
  - `rest`: scale 1
  - `hover`: scale 1.06, transition duration 0.7 ease [0.22, 1, 0.36, 1]

Create `src/components/shop/ProductQuickView.tsx`:
- Overlay component that appears on product card hover (desktop) or tap (mobile)
- Props: `product: Product`, `isVisible: boolean`, `onClose?: () => void`
- Renders as absolute-positioned overlay on the product card (not a modal)
- Content (revealed progressively with stagger):
  1. Product description (first 80 chars with ellipsis) — `text-[11px] text-gray-300 line-clamp-2`
  2. Fragrance notes if available (top/heart/base) — small gold-accented tags
  3. "Quick View" button (link to product page) — subtle gold outline button
- Background: `bg-black/80 backdrop-blur-md` overlay on the image area
- Use `AnimatePresence` and `hoverRevealVariants` for entry/exit
- Close on mouse leave (desktop) or tap outside (mobile)
- Respect `prefers-reduced-motion` — if enabled, show/hide instantly without animation
- 'use client' directive required
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors.
Check exports:
```bash
grep -E "export" src/lib/animations/discovery-animations.ts
grep -E "export" src/components/shop/ProductQuickView.tsx
```
  </verify>
  <done>
- discovery-animations.ts exports all animation variants and timing
- ProductQuickView renders overlay with product details on hover/tap
- Component respects reduced motion preferences
- TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance ProductCard with hover reveals and create DiscoveryGrid</name>
  <files>
src/components/ui/ProductCard.tsx
src/components/shop/DiscoveryGrid.tsx
  </files>
  <action>
Update `src/components/ui/ProductCard.tsx`:
- Read the file first to understand current structure
- Add hover state management: `const [isHovered, setIsHovered] = useState(false)` (requires adding 'use client' if not present)
- Note: ProductCard currently has NO 'use client' — it's a server component using only Link and ProductImage. Converting to client component is needed for hover state.
- Add `onMouseEnter` and `onMouseLeave` handlers to the outer Link
- On hover (desktop):
  - Image zooms slightly using `imageZoomVariants` (scale 1.06 over 700ms)
  - ProductQuickView overlay appears over the image area
- On tap (mobile):
  - First tap shows ProductQuickView, second tap navigates to product
  - Implement with `onClick` handler that calls `e.preventDefault()` on first tap, sets isHovered=true
  - Track `isTapRevealed` state for mobile double-tap pattern
- Wrap image in `motion.div` with `imageZoomVariants` (whileHover on desktop)
- Render `ProductQuickView` positioned absolutely over image container when isHovered
- Keep ALL existing JSX structure — only add hover wrapper and quick view overlay
- Do NOT change any existing styling, pricing display, or badge logic
- Import `useReducedMotion` from existing hooks — disable zoom animation if reduced motion

Create `src/components/shop/DiscoveryGrid.tsx`:
- Enhanced grid wrapper that replaces basic `div.card-grid` in shop pages
- Props: `products: Product[]`, `priority?: number` (how many cards get priority loading, default 4), `className?: string`
- Renders product cards with progressive disclosure pattern:
  - First row (4 items): visible immediately with priority loading
  - Subsequent rows: use IntersectionObserver to load when approaching viewport (200px threshold)
  - Each row fades in with staggered animation using `progressiveDisclosureVariants`
- Grid layout: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-3 md:gap-5` (matching existing)
- Use `useInView` from framer-motion for row-based progressive loading
- Each group of 4 products is treated as one "row" for animation purposes
- Performance: only render ProductCards for rows that are in or near viewport
  - Use a simple `visibleRows` count state that increments as IntersectionObserver fires
  - Start with first 2 rows visible (8 products), reveal more as user scrolls
- 'use client' required
  </action>
  <verify>
Run dev server:
```bash
npm run dev
```
1. Visit http://localhost:3000/shop — hover over a product card — verify image zooms slightly and quick view overlay appears
2. Move mouse away — verify overlay exits smoothly
3. Mobile viewport (375px) — tap a product card — verify quick view appears, second tap navigates
4. Scroll down the product grid — verify rows progressively appear with stagger animation
5. Check with reduced motion — verify instant show/hide, no zoom
6. Run `npx tsc --noEmit` — no TypeScript errors
7. Run `npm run lint` — no lint errors
  </verify>
  <done>
- ProductCard has hover state revealing ProductQuickView overlay
- Image zoom on hover (1.06x over 700ms)
- Mobile tap-to-reveal pattern works (first tap reveals, second navigates)
- DiscoveryGrid progressively reveals product rows on scroll
- Reduced motion disables animations
- All existing ProductCard functionality preserved
- TypeScript and lint pass
  </done>
</task>

</tasks>

<verification>
Manual verification:
1. Hover over product cards — see description preview, fragrance notes, quick view link
2. Hover exit — overlay disappears smoothly
3. Mobile (375px) — first tap reveals overlay, second tap navigates to product
4. Scroll product grid — subsequent rows appear with staggered fade-in
5. Performance: hover interactions are instant (no lag), scroll reveals are smooth 60fps
6. Reduced motion: overlays appear/disappear instantly, no zoom on images
7. All product information (price, brand, sale badge, size) still visible
8. Cart/checkout flow still works (no regression)

Automated:
```bash
npx tsc --noEmit
npm run lint
npm test
```
</verification>

<success_criteria>
Phase 15 Plan 02 complete when:
- [x] ProductCard has contextual hover overlay revealing product info
- [x] Image zoom on hover (1.06x, 700ms luxury timing)
- [x] Mobile tap-to-reveal pattern implemented
- [x] DiscoveryGrid progressively reveals rows on scroll
- [x] Progressive disclosure uses IntersectionObserver for performance
- [x] Reduced motion support for all interactions
- [x] All existing ProductCard features preserved (badges, pricing, links)
- [x] TypeScript compiles, lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-immersive-navigation-discovery/15-02-SUMMARY.md`
</output>
